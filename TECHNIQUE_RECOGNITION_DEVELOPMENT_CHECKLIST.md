# 技巧识别开发清单

## 保留

- 保留普通模式录音完成后点击“技巧识别”的交互入口，按钮与状态文案已经成型。
- 保留技巧选择弹窗的整体结构，只把它继续当作“选择识别项目 + 展示选项”的入口。
- 保留当前 TechniqueRecognitionConfig、TechniquePanelState、BaseTechniqueEvent 及各技巧事件数据结构。
- 保留普通模式画布上的技巧叠加机制，继续使用现有 artist 容器和重绘节流逻辑。
- 保留“无技巧区间不做处理”的行为，不额外标普通演唱段。

## 抽离

- 把离线技巧分析从 GUI 文件中抽到 src/audio_processing 下的独立服务模块。
- 把每个技巧的判定逻辑逐步拆成独立分析器，先从颤音开始。
- 把离线分析结果中的调试信息结构化，方便后续做回归样本比对。
- 把 visualizer 中的分析职责逐步收缩为“接收事件并绘制”，不再承担长期算法演进。

## 重写

- 重写颤音离线识别，不再依赖实时阶段的 vibrato_info 直接下结论，而是基于整段缓存帧的基频轨迹做完整检测。
- 逐步重写滑音、换气、气声、换声区的离线判定逻辑，避免继续在 GUI 内堆叠状态机阈值。
- 重写技巧区间默认样式：默认仅保留两条细竖线和上方标签，区域底色和顶部横线作为可选展示项。

## 当前阶段

- 第一阶段目标：让颤音成为第一种“可调试、可回归、可离线复算”的技巧识别器。
- 当前离线识别输入仍使用录音期间缓存的 FrameFeatures，但判定逻辑已经独立服务化。
- 下一阶段再把输入扩展到录音结束后的整段二次精算，支持 wav 或 pitch json 重跑。

## 颤音验收标准 V1

- 仅在普通模式录音完成后触发。
- 识别结果必须输出开始时间、结束时间、速率、深度、规则性、稳定性、置信度。
- 普通模式画布上必须出现区间左右细竖线和顶部“颤音”标签。
- 无识别结果时不显示任何额外标注。
- 识别器应当可用固定样本重复运行，结果具备可比性。

## 后续顺序

1. 颤音
2. 滑音
3. 换气
4. 气声
5. 换声区